[phases.setup]
nixPkgs = ["chromium", "chromium-chromedriver"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install chromium"]

[start]
cmd = "streamlit run app.py --server.port ${PORT:-8501} --server.address 0.0.0.0"
